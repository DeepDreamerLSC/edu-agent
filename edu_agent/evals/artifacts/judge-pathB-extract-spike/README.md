# 路 B extract-only spike(#253,2026-09-15)——**过门:预注册门命题双跑显式分解,忠实性 65/65 逐字可定位**

点火依据:用户会话内选定(2026-09-15,与 GEPA feasibility / teacher-line 同批);执行序位 = ⑥ 后「(若盲区仍在)路 B extract-only spike」——C40/C11 文本环终止在案,盲区仍在。

## 预注册门(跑前钉死,见驱动 docstring;判据指纹 af93e564 零改动)

| 案 | 门命题(假) | 抽取形态 |
|---|---|---|
| C40 | 「方程两边同时除以3会破坏等式平衡」(规则) | |
| 编造51 | 「八分之一(可)变成十二分之一」(等值转换) | |
| C11(预注册门外扩展,如实标注) | 「(小讲师声称)学生刚才说了三份加一份」(归因;学生实说「占两份,再加一份,共三份」) | |

## 结果(3 案 ×2 独立抽取 + C21 阴性对照 ×2,共 8 调用)

- **门命题双跑显式分解 ✓✓**:
  - C40:run1/run2 均抽出「方程两边同时除以3会破坏等式平衡」(规则类,引文逐字);
  - 编造51:run1 抽出「八分之一可以化成十二分之一」(等值)+「化成后即可相加」(规则),run2 抽出等值命题(引文同源);
  - C11(扩展):run1/run2 均抽出**归因命题**(「学生刚才说过『三份加一份』」)+ **数值命题**(「三份加一份一共是四份」)——归因错位与隐含算术双面显式化;
- **阴性对照 C21**(两跑一致):仅 2 条真命题(路程=60×2.5;忠实归因学生的澄清请求),零幻觉假命题;
- **机械忠实性(外评点名的核心风险)**:8 份抽取全量 65/65 条命题,引文**逐字**可在 transcript 定位(100%;normalized 口径同 100%);
- 稳定性:门命题命中在两跑中一致(案级命中数:编造51 2↔1 为同源命题的规则/等值双形态,均含门命题)。

## 含义(按执行序)

- 抽取忠实性过门 → **正式结构件(抽取层结构化)解锁**,由人裁是否立项;
- 该层与判据层正交:抽取只分解不评判(真假判定在下游),C40 类「规则命题被计算命题吸收」的盲区在抽取层被显式拆出;
- 已知局限(如实):本 spike 为 3+1 案小样;抽取层与判据层的**拼装形态**(两段式判卷 vs 抽取结果入判据 evidence)属正式结构件的设计问题,不在本 spike 面。

## 调用计数(如实)

- live **8 = 4 案 ×2**(C40/编造51/C11 门案 + C21 对照),deepseek-flash(8 行响应 model 全显),全部 attempt=1(路线 1 零修复),零截断;
- **浪费 4 调用已披露**:驱动首版读 `resp.content`(实为 `.text`)致 JSON 解析全空,响应文本未落盘、facts 被后续批 rmtree 覆盖不可复原——按会话记录账面入账(本会话浪费累计:v4-pro 2 + 本批 4);
- 过程纪律:两次 stub 干跑(零 API)验证全链路后才烧正式调用;**自查拦下一处喂答案事故**(首版 SYSTEM 示例即门命题,已换中性示例,干跑断言 SYSTEM 零门命题)。

## 工件

- `extractions-run1.jsonl` / `extractions-run2.jsonl`:4 案 ×2 全量抽取(含全部命题与引文)
- `pathb_extract_driver.py`:驱动存档(SYSTEM/schema/参数;max_tokens 1600 为 spike 参数,披露)
- `api-facts.jsonl`:models 行 + 8 live 调用行(batch 标注)
- `judger.sha256` = `af93e564…`(判据层零改动)

## 复算

```bash
# 需 DEEPSEEK_API_KEY;输入 = judge-v3-pilot-7 存档 4 案(C40/编造51/C11/C21);
# 驱动 = 本目录 pathb_extract_driver.py(judge_independent→flash,schema 路线 1,
# temp=0,max_tokens 1600);×2 独立跑;忠实性核查 = 引文子串定位(本 README 口径)。
```
