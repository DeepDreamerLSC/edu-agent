# 判据1(M4 质量出口)预注册骨架 · 起草冻结稿(dev C;PM 233d0fca 派单 criterion1-prereg-skeleton)

**性质**:预注册骨架——判分协议在**看到任何消融/真候选判分读数之前**冻结;本稿 2026-09-18
落 #333(时间戳=预注册凭证)。含 3 个开放问题(O1-O3)呈 PM 裁;PM 裁后即为冻结正本,
判分开始前禁改。

## 1. 判据正本定位(全文引用,非转述)

**#255(M4 里程碑,body 评审修订版)「判据 1 · 质量出口」原文**:

> - 主指标:**固定 M3 scoring surface 下,批跑总分均值 7.83 → ≥9.40**(同判分器指纹 + 同 93 面;「+20%」为项目语言,不作严格统计声明,不围绕它建新统计体系);
> - **修尺子分支的目标预锁(2026-09-14 人批 30%)**:若 #253 标定触发修尺子(rubric 修订 + rescore 重立基线),新目标 = **T_new = B_new + 0.30 × (12 − B_new)**,B_new 为新指纹下 93 案 rescore 六维总分均值,12 为满分;难度定义 = 吃掉剩余提分空间的 30%(人定,非旧目标对反推);本公式在见到 B_new 之前锁定,rescore 后不再调整;math_integrity 为独立硬门不进此公式;旧 7.83/9.40/指纹留档为 M3 历史记录;
> - 副指标:needs_review 率 27% → ≤15%;数值首过违规率 25.5% → ≤15%;
> - schema 违例操作化:分诊已知 1 例(F1)→ 根因 → 最小回归 → 重复若干次不复现 → 批跑 0;
> - 有效性闸:held-out 4 帧不降(不扩第三套 blind corpus);夜评五维不降;judge 标定已完成。

**修尺子已发生,现行生效线=T_new≥10.2652**,物化证据链:

- `edu_agent/evals/artifacts/judge-v3.2-rescore-93/README.md` 头表:B_new=9.5217(92 案,
  per-case total=(r1+r2)/2)、**T_new=10.2652**(机械式)、指纹
  `af93e5645086f6f02c0fcd303550adedf5a4004b3d8f69359999d481df85d3a8`(与 c5675065011 冻结值
  MATCH)、调用 184=92×2;
- `judge-rubric-v32-migration/README.md` L66:双指纹补注(旧 af93e564 历史纪元+新 9551d149
  后继纪元);`gepa-longrun-01/README.md` L144 同数旁证;
- 细节注:**93 manifest 中 1 案不可判**(fraction_addition_support_boundary,源跑 schema
  违例,全仓无 transcript)→ B_new/T_new 实算于 92 案;判分时若该案仍不可判,照 92 口径
  并在回执声明。

## 2. 判分语料构成

- **headline 正本(判据1 判分面)= 93 manifest 固定面**(corpus-round-v2 cases.jsonl 全集,
  同指纹同面,#255「固定 scoring surface」+「不扩第三套 blind corpus」红线)——T_new 判定
  **只认此面**;形态×长度×图像构成即 manifest 既有构成,不为判分改面。
- **混合系(enriched12 / no_progress / controls)作非 headline 回归切片**:防退化观测用,
  **不计入 T_new 判定**,不进冻结判分面(避免既扩面又自称「同面」的口径矛盾)。
- 学生侧=**冻结剧本**(corpus_round 脚本轮),非模型学生;图像案走生产 vision 路径同款。
- **O1(开放问题,呈 PM)**:混合系是否需要独立冻结档(防后续被误当 headline)?默认=
  只在 README 声明「非 headline」,不另立冻结。

## 3. 预算分解(忠实口径 ≈600;~400 系估算差,见 O2)

| 项 | 算式 | calls |
| --- | --- | --- |
| 判分会话(真候选 tutor 驱动) | 93 面 ×(1 start + 2-4 剧本轮,历史均值 ~3) | ~370 |
| 护栏重生成储备 | 历史首过违规 ~25% 量级,按 10% 储备 | ~40 |
| judge 判分 | 92 可判 × 2 跑(acceptance aggregation 政策,与 B_new 同口径) | 184(+2 若 F1 案修复) |
| **合计** | | **~595** |
| 硬顶 | +10% 裕量 | **650;超顶即停**(run_pairs 式 sys.exit(2),已产工件保留) |

- **O2(开放问题,呈 PM)**:派单估 ~400 与忠实口径 ~595 差 ~200;三选:**a 提额 ~600
  (推荐,保同口径可比)** / b judge 单跑(破聚合可比性,不建议) / c tutor 剧本轮压缩
  (破固定面,不建议)。PM 裁后写死。

## 4. 冻结面清单(判分开始前逐项打勾)

1. judge rubric v3.2 终版文本 + 双指纹注(af93e564 / 9551d149)——不得再修尺子;
2. judge 模型配置(models.yaml judge 角色条目,哈希记录);
3. 聚合政策:两跑平均,per-case total=(r1+r2)/2,均值面=92(或 93 若 F1 修复);
4. **T_new 线 = 10.2652**(本稿 §1 证据链);
5. 93 manifest + input provenance 规则(存档逐字/同构新建规则照 rescore 先例);
6. 剧本学生轮固化(corpus_round fixtures,判分中不改);
7. 判分时 main 指纹(跑时记录,回执披露);
8. 副指标口径:needs_review 率 ≤15%、数值首过违规率 ≤15%、schema 批跑 0、held-out 4 帧
   不降、夜评五维不降——**均照 #255 原文,不另立**。

## 5. 反 gaming 条款

1. 本骨架**先于** B 三臂消融读数被任何一方看到而落档(#333 时间戳);消融/R2 读数见到后
   本骨架只读;
2. 冻结后禁改;任何偏离(语料/预算/口径)须在**判分开始前**呈报留档(#333 评论),未报
   即改=判分作废;
3. 判分对象=**消融收敛 + R2 之后的部署版真候选**——判分跑在部署后,防「边跑边修」;
   判分开始前不得见任何真候选判分读数(T_new 公式先于 B_new 锁定之精神延承);
4. judge/rubric 在判分前后零改动;判分后若不满意读数,唯一合法路径=新预注册+新时间戳。

## 6. 执行触发器(全部满足才开跑)

1. 真候选部署完成(用户部署键;版本=消融收敛+R2 后的 main);
2. 部署后观察窗读数绿(C 监控:r4 拦截无异常漂移、r6 无新增人工异常家族、
   anchor:allowed 无违规在场);
3. B 三臂消融收敛(读数在案)+ R2 过 M3 人审;
4. 本骨架冻结正本在案(PM 裁 O1-O3 后版本)。

## 7. 工件与执行形态

- 本骨架:`edu_agent/evals/artifacts/criterion1-prereg/README.md`(本文件);
- 判分执行器:判分跑时另建 `runs/`(逐案 JSON+guard_events)+ `summary.json`,
  复用 corpus_round.transcript_messages + KernelSubject + judge 两跑模式(teacher-delta-
  review / v1-replay 设施先例);calls 记账=gateway writer 权威计数;
- 回执:判分完成回执落 #333(首行 task=criterion1-scoring),calls/判读对照全披露。

## 8. 三问裁决(PM 233d0fca,criterion1-prereg-freeze-ruling-v1;接回执 5733628619)

- **O1 = 默认**:混合系切片**不立**独立冻结档——README「非 headline」声明即足;
  回归切片是诊断面不是判定面,不扩预注册表面积。
- **O2 = a(提额)**:总 ~600,硬顶 650;派单 ~400 系 PM 记忆估值非正本,忠实算术成立
  (93×~3-4 + 储备 + 92×2 聚合);b(judge 单跑)破坏与 B_new 同口径的两跑聚合、
  c(剧本轮压缩)破坏固定面,均否。**判分跑时仍须用户点火键**(四触发器满足时另呈)——
  预算冻结≠跑权发放。
- **O3 = 默认**:零新统计,照 #255 机械线 **T_new≥10.2652 唯一判定径**——单一判据
  单一路径;两跑聚合已消化 judge 噪声;不加第二验收路径(事后敏感性分析=预注册要防的
  东西);近线结果照预注册读数如实报。

## 9. 冻结声明

**冻结时刻:2026-09-18T17:26:55Z(UTC,系统钟)。本档自本时刻起只读:判分开始前禁改;
任何偏离须在判分开始前呈报 #333 留档;四触发器(§6)满足→呈用户点火→开跑。**
文件指纹见回执(sha256)。
