## 5. 两条修复路径的数据对照表

两路径的**检测**要求相同(都要先认出"方法名/答案数字"并知道它尚未被学生说出),差别在**处置**:A 重调模型重写这一轮;B 直接在文本上删词。

| # | 命中句(截断) | 类型 | 轮次 | 路径A:护栏重生成 | 路径B:解析脱敏(机械删词结果) |
|---:|---|---|---|---|---|
| 1 | 我们来验证一下：5只兔有20只脚，3只鸡有6只脚，加起来是26只脚，和题目一致！… | answer_number | 第3轮 | 可拦(重写该轮;+1 次 tutor 调用;失败→兜底句+`stuck=True`) | **删数字后留空洞/语义断裂** |
| 2 | 你算得很棒，已经知道每换一只鸡为兔脚数多2只，现在差10只脚，那需要换5只兔… | answer_number | 第3轮 | 可拦(重写该轮;+1 次 tutor 调用;失败→兜底句+`stuck=True`) | **删数字后留空洞/语义断裂** |
| 3 | 你用假设法很聪明，先假设全是鸡算出16只脚，再通过每换一只鸡为兔多2只脚，算出需要换5只兔，最后验证脚数正确… | method_name | 总结 | 可拦(重写该轮;+1 次 tutor 调用;失败→兜底句+`stuck=True`) | **删词后留孤儿动词,句子破损** |
| 4 | 你用假设法成功解决了鸡兔同笼问题：先假设全是鸡算出脚数差额，再根据每换一只鸡为兔多2只脚，推算出需要换5只兔，最… | method_name | 总结 | 可拦(重写该轮;+1 次 tutor 调用;失败→兜底句+`stuck=True`) | **删词后留孤儿动词,句子破损** |
| 5 | 那剩下的3只就是鸡啦！… | answer_number | 第3轮 | 可拦(重写该轮;+1 次 tutor 调用;失败→兜底句+`stuck=True`) | **删数字后留空洞/语义断裂** |
| 6 | 你算得真棒！用假设全是鸡的方法，发现实际多出10只脚，每换一只鸡为兔就多2只脚，这说明有5只兔… | answer_number | 第3轮 | 可拦(重写该轮;+1 次 tutor 调用;失败→兜底句+`stuck=True`) | **删数字后留空洞/语义断裂** |
| 7 | 你已经算出面积是30平方厘米，还想知道为什么除以2吗？… | answer_number | 首问 | 可拦(重写该轮;+1 次 tutor 调用;失败→兜底句+`stuck=True`) | **删数字后留空洞/语义断裂** |

### 5.1 代价对照(客观维度)

| 维度 | 路径 A:护栏重生成 | 路径 B:解析脱敏 |
|---|---|---|
| 落点 | 扩 `kernel._guard_check` 加一条规则 + 复用现成 `_regenerate`/`_contextual_fallback` | 新增回复后处理函数(在 `_guard_output` 之后) |
| 额外模型调用 | 每条命中 +1 次 tutor 调用(本轮去重强命中 7 条;实际按命中轮次计) | 0 |
| 失败模式 | 重生成仍违规 → 兜底句**整条替换**教师回复 + `session.stuck=True`(会改 R6 `finish` 的确定性模板通路) | 机械删词后**全部** 7/7 条致损:方法名类 2 条留孤儿动词,数字类 5 条留空洞/语义断裂 |
| 文本完整性 | 保留原轮内容(重写),不产生破损句 | A 类命中**必然**破损(如「你用假设法很聪明」→「你用很聪明」);数字类删后留空洞 |
| 维护面 | 需方法名词表 + 检测规则 + 重写率/失败率实测 | 需同一份词表;无需模型侧规则 |
| 与弧线的冲突 | 弧线要求"他讲完你再点名方法" → 检测须带"学生是否已说出"状态(现 `student_evidence` 已有该信号) | 同上,且脱敏会把**合规的点名**也一起删掉 |

### 5.2 本轮数据能定/不能定的

* 能定:方法名代喂在 11 场景里只出现在**总结句**(两臂皆有);答案数字代喂真实存在且两臂相同(鸡兔同笼第 3 轮教师先说「需要换5只兔」,学生尚未说出)。
* 不能定:两路径的**实际拦截成功率与代价**需要各自实现一版后重跑同一 88 份口径才能测;本轮只提供**语料与机械可拦性**,因此不下路径结论。

### 5.3 实测延迟(给路径 A 的额外调用定价)

本轮 304 次 tutor 轮次调用,中位 **1869 ms**,区间 1081–4980 ms。路径 A 每条命中 +1 次同类调用 → 约 **+1869 ms/命中**。本轮语料共 116 条(含重复),按口径分 **F 50 / P 66**(两口径合计 116);额外调用按命中轮次计,故分别按各口径的命中数计。
