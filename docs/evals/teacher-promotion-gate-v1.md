# 教师线 promotion gate v1(#253,2026-09-15)

**定义来源**:#253 外评收编 c5673905941(人批采纳):「教师线缓解件定义:非『找到老师』,而是 **promotion gate**——最终/短名单 candidate → 固定盲区挑战切片(数学真实性/归因/慈善转述)→ 教师抽审 → 无新增退化 → 才许 promotion。」执行序 ⑥「教师线立即启动」;⑦「GEPA 全量/promotion 前教师盲区 gate 或成熟路 B 至少一个就位」。

本件 = 机制落位:固定切片(11 案,双跑基线已冻结)+ 流程规程。**gate 的运行是按 candidate 触发的,不是一次性**(每个最终/短名单 candidate 过一次门)。

## 一、固定盲区挑战切片(`artifacts/teacher-gate-slice/`)

三家族 11 案,输入逐字冻结于 `slice-cases.jsonl`,双跑基线冻结于 `slice-baseline.jsonl`:

| 家族 | 案 | 角色 | 判据 | 期望(真值) | 基线(af93e564 双跑,携带至 9551d149) |
|---|---|---|---|---|---|
| 数学真实性 | C40 | **已知限**(规则命题被计算命题吸收) | mi | 0 | **2/2**(盲区现状) |
| 数学真实性 | 编造51 | 已修回归位(v3.2 枚举+基底锚点) | mi | 0 | 0/0 |
| 数学真实性 | C15 | 挑战案(中间答案坐标互换) | mi | 0 | 0/0 |
| 数学真实性 | C26 | 正向位(误念修复) | mi | 2 | 2/2 |
| 归因 | C46 | 重裁位(支架后掌握=2) | sm | 2 | 2/2 |
| 归因 | C17 | 正向位(学生证据充分) | sm | 2 | 2/2 |
| 归因 | C25 | 边界位(方法总结在/学生证据缺=1) | sm≥1 | 1 | 1/1 |
| 归因 | C24 | 零参与位(最高 1,实测 0) | sm | 0 | 0/0 |
| 慈善转述 | C11 | **已知限**(转述错位被合理化) | mi | 0 | **2/2**(盲区现状) |
| 慈善转述 | C21 | 泄露语义边界(抖动位) | leak | false | false/false |
| 慈善转述 | C35 | 正向位(终答泄露正确检出) | leak | true | true/true |

基线口径(**双纪元**):基线读数 = 判据指纹 `af93e564…`(历史纪元,v3.2 定稿)下 #268(门 20 双跑)+ #271(92 案双跑,已合 main@`df72e95`)双跑值——C40/C11/C21 三案取自 #271 批;两跑一致(除历史抖动位 C21 本批亦稳)。判据指纹已穿越至 `9551d149…`(#280,继任纪元,现行,锚于切片目录 `judger.sha256`):依 #280 等价验证门①(`SYSTEM_PROMPT`+`DIMENSION_GUIDE` 逐字节相等,24/24)+门②(门字段零翻转),基线**逐案携带**重冻结(`slice-baseline.jsonl` 新增 `baseline_9551d149` 字段,旧 `baseline` 留档);首个 candidate 运行产出新纪元实测读数,与携带基线可比。

**「期望」与「基线」双列的语义**:健康位(基线=期望)是**退化检测器**——candidate 不得使其劣化;已知限位(C40/C11,基线≠期望)的基线就是盲区现状——candidate 不得劣于现状(改善为加分,不构成门的必要条件)。已知的引擎抖动(#253 因子鉴定)由双跑+fail-closed 吸收。

## 二、流程规程(candidate → promotion)

1. **跑**:candidate(任何 prompt 面/判据面改动,含 GEPA 优化产出)在切片 11 案 ×**2 独立调用**(同 acceptance aggregation 口径;引擎 = 判卷引擎梯现役档;不重试不修参数)。复算命令(既有工具,零新脚本):
   `uv run python scripts/rescore_judge.py --archive <collect 源> <题面> --only <切片 id 清单> --out <目录>`(judge 侧直接评 transcript 的 candidate 评测同此口径)。
2. **机械比对**(先于人审,报告按案):健康位两跑均须达期望;mi/leak 两跑不一致 → 保守端计 fail(沿 52 案门协议,fail-closed);已知限位两跑均不得劣于基线。机械红 → 门红,不进人审。
3. **教师抽审**(机械绿才进):抽审集 = 已知限案(C40/C11,**必审**)+ 健康位随机 ≥3 案;教师读 transcript + candidate 分数 + 证据引文,按三家族各自判「无新增退化」(数学真实性:有无新放过的假命题/新误判;归因:学生证据归因有无新错位;慈善转述:转述有无被新放过的错位/新冤枉)。记录沿 b2 人审侧车机制(打分表 + 回执,零新工具)。
4. **判定**:机械 + 教师**双绿** → promotion 许可(合并键仍属人);任一红 → 该 candidate 出局或回炉,结果留档切片目录。

## 三、边界(不做什么)

- 不做常驻 judge、不做自动裁决——教师抽审是人的读案,工具面只有冻结切片 + 机械比对;
- 切片**不替代** 52 案门/93 案基线——那些是判据线的验收面,本切片是 promotion 前的盲区挑战面(三家族定向);
- 切片冻结:案集/基线改动需人批(基线随判据指纹变更而重冻结,重冻结 = 新基线行 + 旧基线留档)。

## 四、对执行序的意义

⑦ 门(GEPA 全量/promotion 前置)的「教师盲区 gate」侧:**机制就位**(本件);实际就位判定 = 首次按 candidate 运行并产出双绿记录。与「成熟路 B」(#272 已过 spike 门,正式结构件未建)为**或**关系——其一完成即 ⑦ 开。
