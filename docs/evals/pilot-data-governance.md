# 试点数据治理(M4 判据② 前置)

**状态**:用户六子项裁定 2026-09-16;本文件 = 治理正本(单一正本,他处只链接)。
**适用范围**:小范围学生试用(濮家六年级;题图 = internal_silver 校内题库)。

## 数据底座(2026-09-16 实测)

- 学生使用链路全本地:tutor = qwen3_vl_8b(127.0.0.1:8303);judge 主选 = mlx_27b(8301,双模型在服务,配置匹配)
- 出机器的点:DeepSeek(本地故障时 fallback + 评测线 judge_independent 10% 平行评分)、阿里云 OSS(题图 + 备份快照);全境内
- facts 日志(model_calls-*.jsonl)**无原文**:纯时延/token/outcome 计量,含 redacted 字段
- 原文存于:SQLite + transcripts.json(Mac 本地)+ OSS 备份快照
- 备份现状:**仓内已有** `deploy/launchd/com.edu-agent.backup.plist`(M3 起,每日 03:30 → OSS),但 **Mac 未加载**(用户级/系统级均无;~/edu-agent-backups/ 最近一次 9/14 = 手动演练)

## 六项裁定

| # | 项 | 裁定 |
|---|---|---|
| ① | 保留期 | 对话与题图**长期保留**(不设到期清理);受②访问边界与⑤删除请求约束 |
| ② | 访问边界 | 运营者全量;开发线默认零,排障经运营者**逐案授权**;教师只看脱敏盲包 |
| ③ | 日志口径 | facts = 技术计量,长期保留;原文归存储层,按本文件管 |
| ④ | 备份 | **部署既有 plist**(仓内已有,缺的是 Mac 加载)+ **14 天滚动**(改 `scripts/backup-db.sh` 或 OSS 生命周期规则);删除传导:清当前存储,旧备份 ≤14 天滚出 |
| ⑤ | 删除请求 | 家长→运营者→Mac 手动删(SQLite 行 + transcripts 条目)→ 删除台账一行(session_id / 时间 / 范围 / 执行人);不建自助系统 |
| ⑥ | 第三方披露 | 知情同意一句话:「对话由本地模型处理;部分评分与备份可能传输至境内云服务(深度求索 / 阿里云 OSS);对话记录将长期保留用于教学改进,您可随时要求删除」 |

## 配套动作

- [ ] 部署既有 backup plist 到 Mac + 补 14 天滚动(plist/script 均已在仓;配置改动合并键在人)
- [ ] (trigger,不空转)若试点督导需教师读真实学生对话:先加脱敏步骤——当前 Lane H 盲包输入 = 评测切片转写(合成学生),不含真实学生数据

## 修订

新裁定 → 新 PR 改本文件;board.json 留 events 指针。
